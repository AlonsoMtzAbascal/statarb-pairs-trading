numpy>=1.24
pandas>=2.0
scipy>=1.10
statsmodels>=0.14
matplotlib>=3.7
pytest>=7.0

# Optional: only needed for the real-data loader (load_prices_yfinance).
# The synthetic demonstration runs without it.
yfinance>=0.2
